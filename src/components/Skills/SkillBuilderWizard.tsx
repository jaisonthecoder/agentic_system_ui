// SkillBuilderWizard.tsx — placeholder until full wizard implementation
import type { Skill } from '../../types'

interface Props {
  onClose: () => void
  onCreated: (skill: Skill) => void
}

export default function SkillBuilderWizard(_props: Props) {
  return null
}
